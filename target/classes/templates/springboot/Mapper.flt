package ${mapperPackage};

import ${modelPackage}.${table.name};

import java.util.Date;
import java.util.List;
/*
 * @author ${author}
 * @email: ${email}
 * @date: ${dateTime}
 * @description: ${table.srcName} ${table.comment} Mapper Generate By Rhythm-2019
 * @version ${version}
*/
public interface ${table.name}Mapper extends Mapper<${table.name}> {

    void insertBatch(List<${table.name}> ${table.name?uncap_first}List);
}